# Generated by Chef for node <%= node['fqdn'] %>

# /etc/aliases
mailer-daemon: postmaster
postmaster: root
nobody: root
hostmaster: root
usenet: root
news: root
webmaster: root
www: root
ftp: root
abuse: root
noc: root
security: root

# chef generated aliases via email_alias LWRP
<% @aliases.each do |name, recipients| -%>
<%= name %>: <%= recipients.join(", ") %>
<% end -%>
